http {
    # ... existing code ...

    # Increase client body size limit to 10GB
    client_max_body_size 10G;

    # Increase proxy buffer size
    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;

    # ... existing code ...
} 